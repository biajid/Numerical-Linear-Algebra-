function [Q, R] = QRFactorization_Householer(A)
% This function performs QR Factorization on a given matrix A, 
% utilization Householder reflection procedure.
[m, n] = size(A);
R = A;
Q = eye(m);

for j = 1 : m - 1
    x = R(:, j);
    a = norm(x);
    e = zeros(m, 1);
    
    e(j) = -a;
    
    v = 