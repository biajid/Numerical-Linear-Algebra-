clear all;
V = 6;
R = 2;
L = 0.5;

t = 0  : 1e-6 : 3;
disp("Loops, with no preallocation of the memory");

tic
for k = 1 : length(t)
    if (t(k) <= 1)
        i(k) = V/R * (1 - exp((-R/L) .* t(k)));
end
